#pragma once

#include "../include/fpga_interface.h"

#define BUFFERSIZE 16*16



class Menu{
	public:
		enum class Position{RESTART_RESUME, LEVEL};
		enum class Level{EASY, MEDIUM, HARD};
		enum class State{STARTED, GAME, PAUSED, FAILED, };

		Menu(char * buf);
		char * GetBuffer(void);
		void SetState(enum State st);
		void SetPosition(enum Position pos);
		void SetLevel(enum Level lv);
		void SetCounter(unsigned int count);
		void Draw(void);

	private:
		unsigned int XPos;
		unsigned int YPos;
		unsigned int Color;
		unsigned int Scale;
		unsigned int Counter;
		enum Position Position;
		enum Level Level;
		enum State State;
		char * Buffer;

};

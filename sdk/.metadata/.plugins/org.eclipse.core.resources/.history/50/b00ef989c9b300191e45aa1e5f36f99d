#pragma once

#define MIN(x, y) ((x < y) ? x : y)
#define MAX(x, y) ((x > y) ? x : y)

#include "stdlib.h"
#define randomInit(seed) srand(seed)
#define random(type, max) ((type)rand() % (max))
#define abs(val) (abs(val)) 

#define enterCritical() (void)
#define exitCritical() (void)

#include "../include/keyboard.hpp"

class Keyboard {

	private:
		bool Keys[];

	public:
		enum class Key {
			KEY_ENTER,
			KEY_LEFT,
			KEY_DOWN,
			KEY_RIGHT,
			KEY_UP,
			KEY_ESC
		};

		bool GetKeyState(Keyboard::Key key);
		bool SetKeyState(Keyboard::Key key, bool state);

};
